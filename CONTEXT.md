# FOCUS

一个极简的复习学习计时工具，用于个人考研备考。核心设计原则：学习就是学习，休息就是休息，不做暧昧判定。

## Language

**学习计时**：
记录一次学习时段的时长。用户主动开始和结束，学习期间可随时填写备注。
_Avoid_: 计次、分段、lap、session

**休息计时**：
记录一次休息时段的时长。可从学习结束后弹出「要休息吗？」进入，也可从空闲状态直接选择「休息」开始计时。休息结束后自动回到学习起始状态。
_Avoid_: 长休息、短休息、手动切换（指学习与休息之间无直接切换）

**模式**：
「学习」或「休息」之一。学习与休息不能直接互切——必须回到空闲态再选择。学习结束后弹出询问，确认后才进入休息。空闲态可选「学习」或「休息」为下一动作。

**科目**：
用户在开始学习前选择的分类，用于标记当前学习时段属于哪个科目。从固定列表中选取或自定义新增。固定列表包含：数学、英语、专业课。仅一级分类。
_Avoid_: 二级子分类、primary_tag、secondary_tag

**标签**：
科目之下的知识点二级细分，用于精细统计与检索（如「今天高数/线代各学了多久」）。扁平全局标签库，任何科目的记录都能挂任何标签；单条学习记录可挂 0..N 个。空库自定义，学习中随时点选/新增，历史记录中可补改。重名自动复用，与科目同名允许（不同维度）。标签库顺序可自定义：⚙ 进入排序模式拖拽换位、完成后批量保存，新标签自动排在末尾。
_Avoid_: 层级标签、标签归属科目、预设标签库

**页数**：
一次学习时段复习的页数（正整数，选填）。学习过程中随时填写/累加（数字框 +「+1/+5/+10」快捷芯片），结束保存时随记录提交；不填则为 NULL，历史页不显示。保存后可在历史记录 ✏️ 编辑态中修改。休息记录没有页数概念。
_Avoid_: 页码范围（如 121-150）、起始页-结束页、预估页数

**复习提醒**：
用户自己维护的复习方法/提醒语句库，用于在学习过程中给自己提个醒（如「复习的关键在于反复多次和全面」）。库里可存多条，学习中「结束学习」大按钮下方以一条小字提醒条展示，每 15 分钟按插入顺序自动轮换到下一条（1→2→3→1…）。学习中提醒条旁点 ＋ 即可弹框新增一条（随时记录）；管理模式内通过弹窗列表对全部条目编辑/删除。数据存于 `reminder_items` 表。仅学习中显示，空闲/休息/暂停均不显示。
_Avoid_: 内置固定语录、手动切换提醒、按标签/科目关联提醒、提醒与学习记录绑定

**学习时段**：
一次完整的「开始 → 结束」学习记录，包含科目、标签、页数、时长和备注。可包含多个学习段和暂停段（由 `segments` 数组记录）。备注在学习过程中填写，保存后可在历史记录中随时修改、补充、清空或点击复制。标签在学习过程中点选，保存后可在历史记录中修改、补充、清空。页数在学习过程中累计，保存后可在历史记录中修改。
_Avoid_: lap、session、计次、分段（用 segments 替代）

**暂停段**：
学习时段内的一个暂停子段，时间计入当日休息统计。在学习中按暂停按钮开始，按继续按钮结束。多个暂停段可出现在同一个学习时段中。每段高度按时间比例在历史记录中以千层饼堆叠条展示。

**休息时段**：
一次完整的休息记录。只记录时长，不需要分类。

**管理模式**：
App 级的隐藏管理开关。正常状态下对用户无感知——连点 5 下右上角考研倒计时（间隔 ≤2s，超时重置）进入（任何页面、任何计时状态都可见），App 层横幅提示「管理模式已开启」，删除类功能解锁：历史页卡片删除按钮、自定义科目删除 ×、标签库删除 × 与 ⚙ 排序。横幅「退出管理模式」按钮退出；刷新页面即复位（不持久，跨 tab 保持）。单条记录删除后不可恢复，学习/休息记录都可删。目的不是权限隔离（单用户工具），而是防误触的入口隔离——所有不可恢复或整理类操作（删除/排序）统一收进模式内，日常界面只留展示与可逆的修改。
_Avoid_: 删除按钮常态可见、软删除/撤销、批量删除、修改类功能收进管理模式（备注/页数/标签修改保持常态入口）

**数据导出**：
管理模式内一键把全部数据导出为 JSON 文件下载的备份功能。覆盖五张业务表（records / subjects / tags / record_tags / reminder_items），顶层带 `app` / `version` / `exported_at` 元数据，`data` 下按表名分组。records 的 `segments` 字段从存储里的 JSON 文本解析为数组（导出物更友好），其余字段保持原值。不含内部表。两种版本都支持且导出格式完全一致、文件互通：服务端版由后端接口生成下载文件，纯静态版由浏览器本地数据层直接生成。
_Avoid_: 按日期范围过滤、CSV / 原始 .db 文件、包含内部表

**数据导入**：
把导出的 JSON 文件恢复到本地的功能，与数据导出配对，语义为「恢复到导出时状态」。事务内先清空五张业务表（records / subjects / tags / record_tags / reminder_items），再按导入数据原样插入——保留原 id，record_tags 的引用关系因此保持一致；不补默认科目，完全信任文件内容。管理模式横幅「导入数据」按钮进入：选文件 → 前端读取并解析、校验顶层结构（`app` = FOCUS、`data` 五表均为数组）→ 确认弹窗展示文件信息与导入统计（学习记录数/科目数/标签数/提醒数）→ 提交。任何一行数据不合法则整个事务回滚、导入不生效。确认弹窗内可先下载当前数据备份；导入成功后提示并整页刷新。学习中（计时非空闲态）不允许导入——先结束当前学习再导入。两种版本导入行为一致：服务端版经 POST /api/import 由后端以 SQLite 约束兜底事务式导入；纯静态版由浏览器本地数据层在 IndexedDB 事务内清空五仓库后原样插入（校验规则与原子性语义对齐）。
_Avoid_: 合并/增量导入、按 id 覆盖、导入时补默认科目、multipart 上传文件

**数据层**：
应用读写数据的统一入口，组件不直接接触存储。两种实现随构建形态二选一：**服务端版**（默认构建）走 REST API → Express → SQLite 五张表；**纯静态版**（`build:static` 构建）走浏览器 IndexedDB 五个对象仓库（与五张业务表 1:1 对应，`record_tags` 独立仓库保存多对多）。对外接口签名、返回形态与错误语义完全一致，组件层零改动。数据语义为「单设备在线 + 手动快照备份/恢复」：设备间同步靠导出/导入 JSON 文件完成，不做多端实时同步、无冲突合并需求。
_Avoid_: 组件直接操作 IndexedDB、多端实时同步、冲突合并

**纯静态版 / 服务端版（双版本）**：
同一套代码库按构建时开关 `VITE_DATA_LAYER`（`local` / `rest`）产出的两种形态：纯静态版（`dist-static`，纯前端，数据存浏览器 IndexedDB）与服务端版（`dist`，沿用现有 Node 服务 + Tailscale 部署）。功能、界面、交互完全一致，仅数据存储位置不同。日常目前默认构建为服务端版，纯静态版作为无后端形态显式 opt-in，两版长期共存、各自演进。纯静态版部署到 GitHub Pages：FOCUS 仓库 `gh-pages` 分支、`https://baobingwen.github.io/FOCUS/` 子路径（`focus` 名字已被源码仓库占用、根地址被其他站占用，故用项目页子路径；GitHub Pages URL 大小写不敏感，小写 `/focus/` 亦可访问），构建时 `vite base` 为 `/FOCUS/`（仅 static 模式）。IndexedDB 按浏览器 origin 隔离：新地址部署后数据为全新起点，旧数据通过导出/导入 JSON 迁移（见 `code/DEPLOY_STATIC.md`）。
_Avoid_: 两套独立代码副本、运行时切换数据层

## Flow

初始状态为空闲态。

### 学习流程

1. 选择科目 → 按「开始学习」
2. 计时走起（纯计时，页面无操作）
3. 计时过程中随时可填写备注、点选/新增标签、累计本次复习的页数，也可在提醒条旁点 ＋ 记录一条复习方法/提醒
4. 按「结束学习」→ 一条学习记录保存（科目 + 标签 + 页数 + 时长 + 备注）
5. 自动弹出「要休息吗？」
   - **是** → 进入休息模式，按「休息结束」→ 一条休息记录保存 → 回到空闲
   - **否** → 直接回到空闲

### 直接休息流程

1. 空闲态选中「休息 ☕」→ 按「开始休息」
2. 休息计时走起
3. 按「结束休息」→ 一条休息记录保存 → 回到空闲

## 技术栈

- 客户端：React 18 + Vite 6 + Tailwind CSS
- 服务端：Express 5 + better-sqlite3（仅服务端版）
- 数据存储：
  - 服务端版：SQLite，单文件 focus.db（位于 `server/data/`）
  - 纯静态版：IndexedDB，`focus-db` 库，五个对象仓库 1:1 对应五张业务表

**计时器状态持久化**：
计时器的运行状态（phase、elapsed、selectedSubject、notes、tags）由 App 组件持有，而非 TimerPage。切换「计时/历史」标签时 TimerPage 卸载重挂，但状态因托管在父级而不丢失。
_Avoid_: 计时状态放在 TimerPage 内
