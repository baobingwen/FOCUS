# FOCUS

一个极简的复习学习计时工具，用于个人考研备考。核心设计原则：学习就是学习，休息就是休息，不做暧昧判定。

## Language

**学习计时**：
记录一次学习时段的时长。用户主动开始和结束，学习期间不计时不做任何操作。
_Avoid_: 计次、分段、lap、session

**休息计时**：
记录一次休息时段的时长。用户主动切换到休息模式后开始计时，结束后自动切回学习模式。
_Avoid_: 长休息、短休息、短休息

**模式**：
「学习」或「休息」之一。互斥，只能处于一种模式。用户主动切换。

**科目**：
用户在开始学习前选择的分类标签，用于标记当前学习时段属于哪个科目。从固定列表中选取或自定义新增。固定列表包含：数学、英语、专业课。仅一级分类。
_Avoid_: 一级标签、二级子标签、primary_tag、secondary_tag

**学习时段**：
一次完整的「开始 → 结束」学习记录，包含科目和时长。没有分段，没有计次。
_Avoid_: lap、session、计次、分段

**休息时段**：
一次完整的休息记录。只记录时长，不需要分类。

## Flow

1. 选择科目 → 按「开始学习」
2. 计时走起（纯计时，页面无操作）
3. 计时过程中随时可填写备注（实时保存）
4. 按「结束学习」→ 一条学习记录保存（科目 + 时长 + 备注）
5. 自动弹出「要休息吗？」
   - **是** → 进入休息模式，按「休息结束」→ 一条休息记录保存 → 回到步骤 1
   - **否** → 直接回到步骤 1

## 技术栈

- 客户端：React 18 + Vite 6 + Tailwind CSS
- 服务端：Express 4 + better-sqlite3
- 数据库：SQLite，单文件 study_timer.db
